import React from 'react';
import ReactDOM from 'react-dom';
import TestApp from './TestApp';

import './includes';

const rootNode = document.getElementById('react');

ReactDOM.render(<TestApp></TestApp>, rootNode);

